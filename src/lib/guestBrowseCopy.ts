export const GUEST_BROWSE_TITLE = 'Browse as Guest';

export const GUEST_BROWSE_MESSAGE =
  'Explore schools, spots, the feed, and comments without an account. Sign up to like, add spots, comment, or report.';

export function formatGuestBrowseMessage(): string {
  return GUEST_BROWSE_MESSAGE;
}
